hyperparameters:
  batch_size : 32 
  block_size : 8 
  max_iters : 5000
  eval_interval : 300
  learning_rate : 1e-3
  # device : 'cuda' if torch.cuda.is_available() else 'cpu'
  eval_iters : 200
  n_embd : 32
  # head_size = 16
  n_layer : 6
  n_head : 4
  dropout : 0.2
  n_train_test_split : 0.9