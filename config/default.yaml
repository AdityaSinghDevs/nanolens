hyperparameters:
  # batch_size : 32 
  # block_size : 8 
  # max_iters : 5000
  # eval_interval : 300
  # learning_rate : 1e-3
  # # device : 'cuda' if torch.cuda.is_available() else 'cpu'
  # eval_iters : 200
  # n_embd : 32
  # n_layer : 6
  # n_head : 4
  # # dropout : 0.2
  # n_train_test_split : 0.9
  # weight_decay : 0.1

#------trained-hparams---
  # batch_size : 64
  # block_size : 256
  # max_iters : 5000
  # eval_interval : 500
  # learning_rate : 3e-4
  # # device = 'cuda' if torch.cuda.is_available() else 'cpu'
  # eval_iters : 200
  # n_embd : 384
  # n_layer : 6
  # n_head : 6
  # # dropout : 0.2
  # n_train_test_split : 0.9
  # weight_decay : 0.1

#--------------new train--colab--T4
  batch_size : 128
  block_size : 256 
  max_iters : 5000
  eval_interval : 300
  learning_rate : 3e-4
  # device : 'cuda' if torch.cuda.is_available() else 'cpu'
  eval_iters : 200
  n_embd : 512
  n_head : 8
  n_layer : 8
  dropout : 0.3
  n_train_test_split : 0.9
  weight_decay : 0.1